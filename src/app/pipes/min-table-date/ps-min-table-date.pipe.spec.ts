import { PsMinTableDatePipe } from './ps-min-table-date.pipe';

describe('PsMinTableDatePipe', () => {
  it('create an instance', () => {
    const pipe = new PsMinTableDatePipe();
    expect(pipe).toBeTruthy();
  });
});
